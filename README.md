## Getting Started
First, run the install package // เริ่มด้วย install package nodemodule
```bash
npm install
# or 
yarn install
```
Second, run the development server:

```bash
npm run dev
# or
yarn dev
# or
pnpm dev
# or
bun dev
```
